name: Auto-merge Agent PRs

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main]

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    # Only run for PRs from cursor/ branches created by trusted users
    if: startsWith(github.head_ref, 'cursor/') && github.actor == 'JonnieSparkles'
    permissions:
      contents: write
      pull-requests: write
      actions: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Auto-merge Agent PR
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔄 Auto-merging PR #${{ github.event.pull_request.number }}..."
          node scripts/auto-merge.js ${{ github.event.pull_request.number }} squash
          echo "✅ PR auto-merged successfully"
          echo "📝 Preparing to trigger deploy workflow..."

      - name: Trigger Deploy Workflow
        uses: actions/github-script@v7
        with:
          script: |
            console.log('🚀 Triggering deploy workflow after auto-merge...');
            
            // Wait a moment for the merge to complete
            await new Promise(resolve => setTimeout(resolve, 2000));
            
            try {
              await github.rest.actions.createWorkflowDispatch({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: 'deploy.yml',
                ref: 'main'
              });
              console.log('✅ Deploy workflow triggered successfully!');
            } catch (error) {
              console.error('❌ Failed to trigger deploy workflow:', error.message);
              // Don't fail the auto-merge if deploy trigger fails
              console.log('⚠️ Auto-merge succeeded but deploy workflow may need manual trigger');
            }
