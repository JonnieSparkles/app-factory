name: 🚀 Deploy to Arweave

on:
  workflow_run:
    workflows: ["Auto-merge Agent PRs"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      message:
        description: 'Deployment message'
        required: false
        type: string
        default: 'Manual deployment via GitHub Actions'

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Only run if the auto-merge workflow succeeded or manual trigger
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    permissions:
      contents: write
      actions: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1  # Shallow clone - we use hash-based change detection

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Configure environment from GitHub Secrets
        env:
          ANT_PROCESS_ID: ${{ secrets.ANT_PROCESS_ID }}
          OWNER_ARNS_NAME: ${{ secrets.OWNER_ARNS_NAME }}
          WALLET_ADDRESS: ${{ secrets.WALLET_ADDRESS }}
          ARWEAVE_JWK_JSON: ${{ secrets.ARWEAVE_JWK_JSON }}
          TURBO_PAYMENT_SERVICE_URL: ${{ secrets.TURBO_PAYMENT_SERVICE_URL }}
          TURBO_UPLOAD_SERVICE_URL: ${{ secrets.TURBO_UPLOAD_SERVICE_URL }}
          APP_NAME: RemoteAgentDeploy
        run: |
          echo "Environment configured."

      - name: Detect Changed Files
        id: changed-files
        run: |
          echo "🔍 Using hash-based change detection in JavaScript deployment code..."
          
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "📝 Manual trigger detected - deploying changed files"
            echo "files=all" >> $GITHUB_OUTPUT
            echo "message=${{ github.event.inputs.message }}" >> $GITHUB_OUTPUT
            echo "deploy_type=apps" >> $GITHUB_OUTPUT
          else
            echo "🔄 Push to main detected - using hash-based detection"
            echo "files=all" >> $GITHUB_OUTPUT
            echo "message=Auto-deployed using hash-based change detection" >> $GITHUB_OUTPUT
            echo "deploy_type=apps" >> $GITHUB_OUTPUT
          fi

      - name: Deploy to Arweave
        id: deploy
        if: steps.changed-files.outputs.files != ''
        env:
          ANT_PROCESS_ID: ${{ secrets.ANT_PROCESS_ID }}
          OWNER_ARNS_NAME: ${{ secrets.OWNER_ARNS_NAME }}
          WALLET_ADDRESS: ${{ secrets.WALLET_ADDRESS }}
          ARWEAVE_JWK_JSON: ${{ secrets.ARWEAVE_JWK_JSON }}
          TURBO_PAYMENT_SERVICE_URL: ${{ secrets.TURBO_PAYMENT_SERVICE_URL }}
          TURBO_UPLOAD_SERVICE_URL: ${{ secrets.TURBO_UPLOAD_SERVICE_URL }}
          APP_NAME: RemoteAgentDeploy
        run: |
          echo "🚀 Deploying changed files..."
          echo "📁 Files to deploy: ${{ steps.changed-files.outputs.files }}"
          echo "💬 Message: ${{ steps.changed-files.outputs.message }}"
          echo "🎯 Deploy type: ${{ steps.changed-files.outputs.deploy_type }}"
          
          # Initialize deployment tracking
          DEPLOYMENT_SUCCESS=true
          DEPLOYMENT_ERRORS=""
          echo "deployed=false" >> $GITHUB_OUTPUT
          
          # Handle deployments using incremental detection
          if [ "${{ steps.changed-files.outputs.deploy_type }}" = "apps" ]; then
            # Handle deployments - deploy only changed files using incremental detection
            if [ "${{ steps.changed-files.outputs.files }}" = "all" ]; then
              echo "📱 Deploying changed files using incremental detection..."
              # Use incremental deployment for each app directory
              find apps/ -type d -mindepth 1 -maxdepth 1 | while read -r app_dir; do
                app_name=$(basename "$app_dir")
                echo "📁 Checking app: $app_name"
                if ! node deploy.js --file "$app_dir" --message "${{ steps.changed-files.outputs.message }}"; then
                  DEPLOYMENT_SUCCESS=false
                  DEPLOYMENT_ERRORS="$DEPLOYMENT_ERRORS\n❌ Failed to deploy app: $app_name"
                fi
              done
            else
              # Handle specific files
              FILES_LIST=$(echo "${{ steps.changed-files.outputs.files }}" | tr ' ' '\n')
              APPS_FILES=$(echo "$FILES_LIST" | grep '^apps/' || true)
              
              # Deploy specific files and directories
              if [ -n "$APPS_FILES" ]; then
                echo "📱 Deploying specific files and directories..."
                echo "$APPS_FILES" | while read -r file; do
                  if [ -f "$file" ] || [ -d "$file" ]; then
                    echo "📤 Deploying: $file"
                    if ! node deploy.js --file "$file" --message "${{ steps.changed-files.outputs.message }}"; then
                      DEPLOYMENT_SUCCESS=false
                      DEPLOYMENT_ERRORS="$DEPLOYMENT_ERRORS\n❌ Failed to deploy: $file"
                    fi
                  fi
                done
              fi
            fi
          fi
          
          # Report deployment results
          if [ "$DEPLOYMENT_SUCCESS" = "true" ]; then
            echo "✅ All deployments completed successfully!"
            echo "deployed=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Some deployments failed:"
            echo -e "$DEPLOYMENT_ERRORS"
            exit 1
          fi

      - name: Commit deployment logs and manifests
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Add logs if they exist
          if [ -f "logs/deployments.json" ]; then
            git add logs/
          fi
          
          
          # Commit if there are changes
          if ! git diff --cached --quiet; then
            git commit -m "Update deployment logs" || echo "No changes to commit"
            git push origin main || echo "Nothing to push"
          else
            echo "No changes to commit"
          fi

      - name: Trigger Announce Workflow
        if: success() && steps.deploy.outputs.deployed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            console.log('📢 Triggering announce workflow...');
            
            // Get the deployment hash from logs
            const fs = require('fs');
            let deploymentHash = '';
            let filePath = 'deploy/hello-world.txt';
            
            try {
              if (fs.existsSync('logs/deployments.json')) {
                const logs = JSON.parse(fs.readFileSync('logs/deployments.json', 'utf8'));
                const latest = logs[logs.length - 1];
                if (latest && latest.success && (latest.undername || latest.commitHash)) {
                  deploymentHash = latest.undername || latest.commitHash;
                  filePath = latest.filePath || 'deploy/hello-world.txt';
                }
              }
            } catch (e) {
              console.log('⚠️ Could not read deployment logs:', e.message);
            }
            
            if (deploymentHash) {
              await github.rest.actions.createWorkflowDispatch({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: 'announce.yml',
                ref: 'main',
                inputs: {
                  deployment_hash: deploymentHash,
                  file_path: filePath,
                  announce_type: 'discord',
                  custom_message: 'Auto-announced after successful deployment'
                }
              });
              console.log('✅ Announce workflow triggered successfully with hash:', deploymentHash);
            } else {
              console.log('⚠️ No deployment hash found, skipping announce trigger');
            }